# Copy this file to .streamlit/secrets.toml and fill in your Anthropic key
# to enable the chat tab. Without a key the chat tab falls back to a static
# report card of the structured retrieval context.

anthropic_api_key = "sk-ant-YOUR-KEY-HERE"
